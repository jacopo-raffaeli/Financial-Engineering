function last_discount = get_ld(matrix,n)
% Select the last element of the n-th row of the matrix
%
% INPUT:
% matrix
% n: index of row
%
% OUTPUT:
% last_discount

last_discount = matrix(n, end);

end